<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@mipmap/icon_bg_login"
    android:clipToPadding="true"
    android:fitsSystemWindows="true">

    <EditText
        android:id="@+id/et_username_reg"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="20dp"
        android:layout_marginLeft="20dp"
        android:layout_marginRight="20dp"
        android:layout_marginTop="150dp"
        android:background="@drawable/bg_edittext"
        android:cursorVisible="true"
        android:hint="请输入账号"
        android:padding="10dp"
        android:singleLine="true" />

    <EditText
        android:id="@+id/et_password_reg"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/et_username_reg"
        android:layout_margin="20dp"
        android:background="@drawable/bg_edittext"
        android:cursorVisible="true"
        android:hint="请输入密码"
        android:inputType="textPassword"
        android:padding="10dp"
        android:singleLine="true" />

    <EditText
        android:id="@+id/et_password_confirm_reg"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/et_password_reg"
        android:layout_margin="20dp"
        android:background="@drawable/bg_edittext"
        android:cursorVisible="true"
        android:hint="请确认密码"
        android:inputType="textPassword"
        android:padding="10dp"
        android:singleLine="true" />

    <CheckBox
        android:id="@+id/cb_remember_reg"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/et_password_confirm_reg"
        android:layout_marginLeft="20dp"
        android:text="记住用户"
        android:textSize="16sp"
        android:visibility="gone" />


    <Button
        android:id="@+id/btn_regist"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/cb_remember_reg"
        android:layout_margin="20dp"
        android:layout_weight="1"
        android:background="@drawable/selector_button"
        android:text="注册"
        android:textSize="20sp" />

</RelativeLayout>